apply plugin: "war"

bootWar {
    mainClass = "com.polarbears.capstone.hmscustomer.HmscustomerApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
